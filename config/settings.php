<?php

return array(

	/* Frequency of AJAX version requests */
	'update_milliseconds' => 1000,

	/* Environments (LARAVEL_ENV) where rejiggering is allowed     */
	/* e.g. 'local', 'dev', 'stage', but probably not 'production' */
	/* '*' is wildcard, turned on by default for ease of use       */
	'environments' => array(
		'*',
	),

);